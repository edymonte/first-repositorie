name: first pipeline
on:
   push:
     branches: ["main"]
   workflow_dispatch:
jobs:
  first-job:
    name: "My first job"
    runs-on:  ubuntu-latest
    steps:
      - name: "My first action"
        run: echo "Hello World !!!!"
      - name: "Secound action"
        run: |
          echo "You create his first workflow"
          echo "Congratulations !!!!!!!!!!"
      - name: "Script Python"
        run: |
          variavel = "Hello World"
          print(variavel)
        shell: python


    
     
     
